<div class="container">
  <h2 ><%= @task.title %></h2>
  <p><%= @task.details %></p>
  <p><%= @task.completed %></p>

  <%= link_to "Update task", edit_task_path(@task.id) %>
  <%= link_to "Back to all tasks", tasks_path %>
</div>
